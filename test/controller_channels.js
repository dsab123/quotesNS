'use strict';

var sinon = require('sinon');
var chai = require('chai');
var expect = chai.expect;
var mockery = require('mockery');
var channels;

describe("Channels Controller: ", function() {
    // mocks, stubs
    

    beforeEach(function() {

    });

    afterEach(function() {

    });

    describe("channels.create", function() {
        it("should interact with the model", function() {

        });

        it("should throw error if the channel is already created") {
            // or return 200 with info?
        });

        it("should return 200 status code if channel is created successfully") {

        });
    });

});
